from setuptools import setup, find_packages

setup(
    name='unione',
    version='0.1',
    packages=['unilite'],
    install_requires=[
        'web3',
    ],
    # other metadata (author, description, etc.)
)
