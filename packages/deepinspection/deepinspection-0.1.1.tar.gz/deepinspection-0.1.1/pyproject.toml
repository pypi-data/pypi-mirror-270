[tool.poetry]
name = "deepinspection"
version = "v0.1.1"
description = ""
authors = ["NextML"]
readme = "README.md"
packages = [
    { include = "deepinspection" }
]

[tool.poetry.dependencies]
python = "^3.9,<3.11"
requests = "^2.31.0"
pydantic = "^2.6.0"

[tool.poetry.group.dev.dependencies]
black = "^24.1.1"
pytest = "^8.0.0"
ipykernel = "^6.29.0"
ipywidgets = "^8.1.1"
pydantic-settings = "^2.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
