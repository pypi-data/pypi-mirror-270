[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "packmetric"
authors = [
    {name = "e_yi", email = "e_yi@outlook.com"}
]
description = "PackMetric"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "lightning>=2",
    "torch>=2.0.0",
    "torchmetrics>1.3.0"
]