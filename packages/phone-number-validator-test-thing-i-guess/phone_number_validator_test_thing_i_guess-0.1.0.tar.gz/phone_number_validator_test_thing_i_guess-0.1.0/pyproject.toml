[tool.poetry]
name = "phone-number-validator-test-thing-i-guess"
version = "0.1.0"
description = ""
authors = ["Isaac Mierow <mierowir@rose-hulman.edu>"]
readme = "README.md"
packages = [{include = "phone_number_validator"}] # Added based on tutorial.

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.31.0"


[tool.poetry.group.test.dependencies]
pytest = "^8.2.0"
requests-mock = "^1.12.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
