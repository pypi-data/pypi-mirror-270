[tool.poetry]
name = "llamaindex-py-client"
version = "0.1.19"
description = ""
authors = ["Logan Markewich <logan@runllama.ai>"]
readme = "README.md"
packages = [{include = "llama_index_client"}]

[tool.poetry.dependencies]
python = ">=3.8,<4"
pydantic = ">=1.10"
httpx = ">=0.20.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
