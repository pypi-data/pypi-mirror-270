[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cloudacious-iac"
version = "0.0.1"

[tool.hatch.build.targets.wheel]
packages = [
    "src/cloudacious",
    ]
