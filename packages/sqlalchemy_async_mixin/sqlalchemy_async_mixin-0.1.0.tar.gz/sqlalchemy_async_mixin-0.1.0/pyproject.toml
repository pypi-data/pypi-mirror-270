[project]
name = "sqlalchemy_async_mixin"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    { name = "PdcShoun", email = "pdc.pachara@gmail.com" },
]
dependencies = [
    "sqlalchemy[asyncio]>=2.0.29",
]
requires-python = ">=3.8"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
