[package]
name = "virtual-wasi-build"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

# list of all WASI components we build
virtual-wasi-cli = { path = "../cli" }
virtual-wasi-clocks = { path = "../clocks" }
virtual-wasi-filesystem = { path = "../filesystem" }
virtual-wasi-io = { path = "../io" }
virtual-wasi-random = { path = "../random" }

[build-dependencies]
wit-component = { version = "0.205", default-features = false }

[lints]
workspace = true
