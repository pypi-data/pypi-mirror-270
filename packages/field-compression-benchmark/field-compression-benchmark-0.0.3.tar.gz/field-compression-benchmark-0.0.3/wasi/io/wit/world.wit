package fcbench:wasi@0.2.0;

interface stdio {
  // FIXME: remove dummy Ok(u8) value
  write-stdout: func(contents: list<u8>) -> result<u8, string>;
  flush-stdout: func() -> result<u8, string>;

  write-stderr: func(contents: list<u8>) -> result<u8, string>;
  flush-stderr: func() -> result<u8, string>;
}

world virtual-io {
  import stdio;
  export wasi:null/io@0.2.0;

  export wasi:io/error@0.2.0;
  export wasi:io/poll@0.2.0;
  export wasi:io/streams@0.2.0;
}
