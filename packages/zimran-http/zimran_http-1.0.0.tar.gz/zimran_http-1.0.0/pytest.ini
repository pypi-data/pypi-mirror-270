[pytest]
python_files = test*.py
asyncio_mode = auto
addopts = --verbose
          --show-capture stdout
          --disable-warnings
          --cov zimran
          --cov-report term

pythonpath = .
